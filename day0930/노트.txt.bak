**git의 사용
1. 크롬 브라우저에서 "download git'을 검색하여 다운받아 설치
-------------------------------------------------
**오류의 종류
1.문법오류
자바문법 규칙에 맞지 않는 오류이며 컴파일이 되지 않음
컴파일 시 오류 메시지 출력됨
오류메시지를 보고 오류해결 후 다시 컴파일하기

2.논리오류
자바문법은 올바르지만 논리적으로 문제해결을 잘못 판단하여
프로그램이 작성되어 원하는대로 동작하지 않음
이런경우를 "버그"(벌레가 숨어있다)가 있다라고 말하며
이러한 버그를 해결하는 과정을 "디버깅"한다라고 함

3.실행오류
자바문법도 올바르고 잘 실행되는 프로그램이지만
사용자의 부주의로 인하여 예를 들어 정수를 입력해야 하는 곳에서
문자열을 입력한다던지 하는 사용자의 프로그램 사용 미숙 등으로 인하여
발생하는 오류 
이러한 경우에는 예외처리를 통하여 문제를 해결할 수 있음

--------------------------------------------------
**이름 작성의 규칙
클래스이름이나 변수이름이나 메소드이름을 정해줄 때는
가능하면 의미있는 이름으로 정해주어야 하고 사용자가 마음대로 정할 수 있음

그러나 다음의 내용은 반드시 지켜야 함
1)클래스이름이나 변수이름이나 메소드이름의 첫글자는 영문이거나
_기호, $기호만 사용가능하나 특별한 경우가 아니면 기호는 사용하지 않음
반드시 영문으로 시작해야 함

2)클래스이름의 첫글자는 대문자로 시작
3)변수이름이나 메소드이름의 첫글자는 소문자로 시작

4)클래스이름이나 변수이름, 메소드이름을 의미있는 이름으로 주다보면
두 개 단어이상을 합쳐야 하는 경우가 있는데 이런 경우 두번째 단어 이후에
시작하는 각 단어의 첫글자는 대문자로 작성
ex)변수,메소드: myAge 처럼 사용 / 클래스: MyAge 처럼 사용

5)클래스이름이나 변수이름, 메소드이름에는 공백문자를 포함할 수 없음
ex)변수 my age (x)
폴더를 만들 때에도 가급적이면 공백문자를 사용하지 않도록 함
---------------------------------------------------
**주석문
프로그램의 실행에 영향을 끼치지 않고 설명을 하기 위한 문장
1)한줄 주석:
//로 시작하며 거기서부터 그 줄 끝까지는 주석문으로 처리됨

2)여러줄 주석:
/*로 시작하고 */로 끝나는 데까지의 여러줄을 주석문으로 처리함

주석문은 협업을 위한 다른 개발자나 미래의 후임자나 
혹은 나 자신을 위해서도 설명문이 필요할 때 사용함

논리오류를 제거하기 위한 디버깅을 할 때에도
의심되는 코드를 주석문으로 처리해가면서 활용할 수 있음
--------------------------------------------------
**제어문
프로그램의 흐름을 제어하는 문장들을 말함
1.선택문
2.반복문
3.break, continue

1)선택문
프로그램의 흐름에 따라 혹은 상황에 따라 실행시키고자 하는 문장을 
선택하기 위한 문장을 말함
-if문
-switch case문

[if문]
조건식에 따라 실행시키고자 하는 문장을 선택할 때 사용
조건식이 참일 때 실행할 문장과 거짓일 때 실행할 문장이 서로 달라지게
사용할 수도 있고, 조건식이 참인지만 판별할 수도 있고,
여러가지 조건식을 차례로 판별하도록 다양하게 사용할 수 있음

<기본형식>
if(조건식)
    조건식이 참일 때 실행할 명령어;
else
    조건식이 거짓일 때 실행할 명령어;
------------
ex)아래의 경우 a와 b를 순차적으로 실행한 다음 조건식이 참이라면 c를 실행하고,
그 다음 e와 f를 차례로 실행함 or
이 경우 a와 b를 순차적으로 실행한 다음 조건식이 거짓이라면 d를 실행하고,
그 다음 e와 f를 차례로 실행함
즉! a,b,e,f는 참이던 거짓이던 수행하고자 하는 명령어기 때문에
if문 바깥으로 표현하는 것이 더 좋음

a;
b;
if(조건식)
      c;
else
      d;
e;
f;
--------------
ex)아래 코드는 만약 조건식이 참이라면 어떠한 문장을 실행할까?
a,b,c,d,g,h를 실행하기를 기대하겠지만 오류가 발생한다
왜냐하면 만약 조건식이 참일 때 명령어가 c,d처럼 두 가지 이상의
명령어를 실행시켜야 한다면 중괄호를 넣어야 함 

a;
b;
if(조건식)
     c;
     d;
else
     e;
     f;
g;
h;

수정코드)아래 코드처럼 표현해야 조건식이 참일 때 a,b,c,d,g,h를 실행함
조건식이 거짓이면 a,b,e,f,g,h 실행함

a;
b;
if(조건식){
     c;
     d;
}
else{
     e;
     f;
}
g;
h;
----------
ex)아래의 경우 a,b실행하고 조건식1을 판별함
조건식1이 참이면 c,f,g 실행함
a,b실행하고 조건식1을 판별했더니 거짓이면 조건식2를 판별함
조건식2가 참이면 d,f,g 실행함
a,b실행하고 조건식1를 판별했더니 거짓이면 조건식2를 판별했는데,
조건식2도 거짓이면 조건식3을 판별함
만약 조건식3이 참이면 e,f,g 실행함
a,b실행하고 조건식1,2,3판별했는데 3개 다 거짓이면
k,f,g 실행함
아래와 같이 판단해야 하는 경우의 수가 여러가지라면 if,else if, else로 표현가능

a;
b;
if(조건식1){
    c;
}else if(조건식2){
    d;
}else if(조건식3){
    e;
}else{
    k;
}
    f;
    g;
--------------
ex)a,b실행하고 조건식1 판별함. 조건식1이 참이면 그 안에서 조건식2판별함
만약 조건식2도 참이면 c를 실행하고, 조건식1은 참인데 조건식2는 거짓이면 
d,g,h를 실행함
a,b실행하고 조건식1 판별함. 만약 거짓이라면 else에 있는 조건식3을 판별함
만약 조건식3이 참이면 e,g,h를 실행함
조건식1도 거짓이고 조건식3도 거짓이라면 f,g,h실행함

a;
b;
if(조건식1){
     if(조건식2){
          c;
     }else{
          d;
}
}else{
     if(조건식3){
          e
     }else{
          f;
     }
}
g;
h;
----------------
ex)a.b를 실행한 다음 조건식을 판별하여 조건식이 참이라면 c,d,e를 실행함
a,b를 실행한 다음 조건식이 거짓이면 d,e를 실행함
if문이 있다고 해서 반드시 else가 있어야 하는 것은 아님

a;
b;
if(조건식){
     c;
}
d;
e;















