/*ForExam0502 관련
int sum = 0;  
*/

class ForExam0503
{
	public static void main(String[] args) 
	{
		int sum;
		int i=1;
		sum = sum + i;
		/*위 코드도 오류발생
		sum변수 선언만 하고 아무값도 주지 않았기 때문에
		sum+i라는 연산 수행할 수 없음
		연산을 수행시키기 위해서는 초기값을 설정해야 함!
		*/

	}
}
